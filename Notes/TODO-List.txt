Sometimes float values in stats can become 0.9999999999

UI doesn't scale with screen on FreeAspect (specifically in battle)

What happens when stat is reduced to 0?

Battle must be more readable: MoveTelegraphing is not readable (bigger / better colors / font)

AI
Server

Colors are somewhere hardcoded. Maybe there should be a theme that controls colors
Gameplay numbers are rounded and otherwise transformed to view numbers in different places. Maybe that should be unified

I'm afraid that on every load we're creating new controllers. For example RewardController.Initialize allocates new controller while the old one might still exist

Maybe MoveLoadoutService should manipulate domain data directly

SceneLoader and ScreenLoader
TurnView and TurnService to show who is on turn when
Battle should be broke down into multiple MVC-s and maybe even have BattleStateMachine
Show how much health each turn removed from enemy
[CHECK] Maybe BatlleMoveItemView and MoveManagementMoveItem and RewardItemView should be either prefab variants or in some way extensions of MoveListItem

Architectural questions:
Can game be played without Controllers and Views 
Does everything depends on interfaces?

Couldn't replicate:
If player uses stat modifier and it doesn't expire it stays applied after the battle ends
